def kemija():
    message = input()

    decode = ""

    i = 0

    while i < len(message):
        decode = decode + message[i]
        if message[i] in "aeiou":
            i += 3
        else:
            i += 1

    print(decode)


kemija()